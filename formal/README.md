# Formalization

Lean and Rocq developments will be peers under this tree. Neither foundation
silently defines the other's semantics.

The first non-authoritative experiment is under `lean/E0`. It is excluded from
the default gate until the exact Lean foundation is bound; run it explicitly
with `make check-e0-lean`.
