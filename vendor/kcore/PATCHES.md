# Patches to the vendored KCore core

Every change Seki makes to an upstream file is listed here, with its reason,
and recorded in `UPSTREAM.json` under `patches` with the file's patched digest.
Each is committed on its own and is a candidate for the spun-off KCore product.

None yet.
