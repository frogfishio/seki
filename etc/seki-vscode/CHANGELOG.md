# Change Log

## [0.0.1]

- Initial release: TextMate grammar and language configuration for `.seki`
  files, covering the constructs in `spec/language/seki-v0.peg` plus the
  `bytes[..]` literal used by `spec/language/examples/coverage_gaps.seki`.
